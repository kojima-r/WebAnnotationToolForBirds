class AnnotaionCommon {
    CANVAS_WIDTH = 1000;
    CANVAS_HEIGHT = 250;
    sampleRate: number;
    wavDuration: number;
    baseFilename = "default";
    //
    audioView: AudioView;
    labelView: LabelView;
}